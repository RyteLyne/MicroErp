#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../admin-lte-express/bin/generate" "$@"
  ret=$?
else 
  node  "$basedir/../admin-lte-express/bin/generate" "$@"
  ret=$?
fi
exit $ret
